%h1= group.name

%h2= group.description
%h2= group.invitation_code

%h3 Members:

- group.users.each do |user|
  %li= link_to user.name, user
  - group.attributes.each do |key, value|
    - if value == true
      %li= user.send("#{key}")


